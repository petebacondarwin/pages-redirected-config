{
  "name": "pages-redirected-config",
  "private": true,
  "description": "",
  "license": "ISC",
  "author": "",
  "main": "src/index.js",
  "scripts": {
    "build": "node -r esbuild-register tools/build.ts",
    "postinstall": "pnpm build",
    "check:type": "tsc",
    "dev": "pnpm run build && wrangler pages dev"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild-register": "^3.6.0",
    "wrangler": "https://prerelease-registry.devprod.cloudflare.dev/workers-sdk/prs/7442/npm-package-wrangler-7442"
  }
}
